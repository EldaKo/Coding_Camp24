#include <stdio.h>

int main() {
	float under, height;
	printf("밑변, 높이 : ");
	scanf_s("%f %f", &under, &height);
	float area;
	area = (under * height) / 2;
	printf("밑변 : %f 높이 : %f인 삼각형의 면적 : %.2fcm", under, height, area);
}
